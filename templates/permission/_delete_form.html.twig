<form method="post" action="{{ path('app_permission_delete', {'id': permission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ permission.id) }}">
    <button class="btn">Delete</button>
</form>
